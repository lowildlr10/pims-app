'use client';

import { Stack, Title } from '@mantine/core';
import React from 'react'

const DashboardClient = ({}) => {
  return (
    <Stack p={20}>
      <Title order={2}>Welcome back, Lowil</Title>
    </Stack>
  )
};

export default DashboardClient;